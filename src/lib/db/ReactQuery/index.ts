"use client";

import { sessionType } from "@/components/Header";
import { useMutation } from "@tanstack/react-query";
import { CreateUserAction } from "../actions/mutation.action";

export const useUserMutation = () => {
	const mutation = 

	return mutation;
};
